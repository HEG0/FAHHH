ls -l ~/.zshrc

if no zshrc file or rw privlages only for root, install oh my zsh(sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)") and then:

install: curl -fsSL https://raw.githubusercontent.com/HEG0/FAHHH/main/install.sh | bash

uninstall: curl -fsSL https://raw.githubusercontent.com/HEG0/FAHHH/main/uninstall.sh | bash (it gets annoying after a while)
